specVersion: "v1beta"
serviceId: "firebase-course"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebase-course-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
